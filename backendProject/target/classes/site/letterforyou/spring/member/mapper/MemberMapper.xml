<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.letterforyou.spring.member.mapper.MemberMapper">
	
	
	<select id="getUserByUserId" parameterType="string" resultType="site.letterforyou.spring.member.domain.UserVO">
	
	select user_id as userId,
	user_name as userName,
	user_email as userEmail,
	user_nickname as userNickname,
	user_image as userImage
	
	 from tbl_user where user_id = #{userId}
	</select>
	
	
</mapper>